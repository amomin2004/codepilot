{
  "version": 2,
  "buildCommand": "cd web && npm install && npm run build",
  "outputDirectory": "web/.next",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_API_URL": "@codepilot_api_url"
  },
  "build": {
    "env": {
      "NEXT_PUBLIC_API_URL": "@codepilot_api_url"
    }
  }
}

